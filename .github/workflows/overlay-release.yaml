name: Overlay Release

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - overlay/*
    paths:
      - 'overlay/version.txt'

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  release:
    name: Release Overlay Component
    permissions:
      packages: write
      security-events: write
    uses: ./.github/workflows/release-template.yaml
    with:
      component: 'overlay'
      build-target: 'build'
      deploy-target: 'deploy'
      requires-docker-login: true
